plugins {
    id 'java'
}

group 'com.github.rpjvanaert'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.3'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.3'
    testImplementation 'org.mockito:mockito-core:5.3.1'
}

tasks.withType(Test) {
    useJUnitPlatform()
}

tasks.withType(JavaCompile) {
    sourceCompatibility = '17'
    targetCompatibility = '17'
}

// Plugin configuration (can be enabled when IntelliJ plugin compatibility is resolved)
// To build as IntelliJ plugin, add: id 'org.jetbrains.intellij' version '<compatible-version>'

